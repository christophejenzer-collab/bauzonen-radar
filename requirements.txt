# Geodaten-Verarbeitung
geopandas>=0.14
shapely>=2.0
pyproj>=3.6

# WMS/WFS-Zugriff
owslib>=0.29
requests>=2.31

# Karten und Visualisierung
folium>=0.15

# GUI (eine der beiden – beim Projektstart festlegen)
guizero>=1.5
# streamlit>=1.30

# Geocoding (Adresse → Koordinaten)
geopy>=2.4

# Tests
pytest>=7.4

# Optional für PDF-Export (Erweiterung)
# reportlab>=4.0
